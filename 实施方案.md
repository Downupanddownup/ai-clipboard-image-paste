# 截图转路径增强粘贴实施文件

## 目标

落地两个真实文件：

- [Save-ClipboardImageToAiCache.ps1](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/Save-ClipboardImageToAiCache.ps1)
- [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)

行为定义：

- `Ctrl + V` 保持原样
- `Ctrl + Shift + V` 作为增强粘贴
- 剪贴板里是截图图片时，插入 `image: "<绝对路径>";`
- 剪贴板里不是图片时，回退为普通 `Ctrl + V`
- 不覆盖原剪贴板内容

## 环境

本机已确认安装：

- AutoHotkey `2.0.2`
- 路径：`C:\Program Files\AutoHotkey\v2\AutoHotkey.exe`

实现基于 AutoHotkey v2，不兼容 v1 语法。

## 文件布局

脚本放仓库内，便于维护：

- [script](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script)

其中：

- [Save-ClipboardImageToAiCache.ps1](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/Save-ClipboardImageToAiCache.ps1)
- [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)

运行缓存放用户目录，不进仓库：

- `C:\Users\<你>\.ai-clipboard-image-cache\`

## 脚本职责

### `Save-ClipboardImageToAiCache.ps1`

职责：

- 在 Windows PowerShell 5.1 的 `-Sta` 模式下读取剪贴板图片
- 自动创建缓存目录
- 将图片保存为 PNG
- 将最终绝对路径输出到标准输出

直接运行方式：

```powershell
powershell.exe -NoProfile -Sta -ExecutionPolicy Bypass -File "D:\PersonWorkSpace\PasePictureForKimiCodeAndClaudeCode\script\Save-ClipboardImageToAiCache.ps1"
```

成功时会输出类似：

```text
C:\Users\你的用户名\.ai-clipboard-image-cache\shot-20260527-180500.png
```

### `ai-clipboard-image-paste.ahk`

职责：

- 监听全局热键 `Ctrl + Shift + V`
- 如果剪贴板不是图片，发送普通 `Ctrl + V`
- 如果剪贴板是图片，调用上面的 PowerShell 脚本
- 获取图片路径并直接向当前焦点发送：

```text
image: "C:\Users\你的用户名\.ai-clipboard-image-cache\shot-20260527-180500.png";
```

## 为什么必须用 AutoHotkey v2

不是因为 v1 理论上做不到，而是当前机器和这套脚本都应该直接按 v2 落地。

原因只有三个：

- 你本机装的是 v2.0.2
- 当前脚本使用的是 v2 语法
- 新脚本没有必要为旧版本兼容付出维护成本

如果用 v1，等于要重写热键、函数、字符串和命令调用写法，没有收益。

## 如何安装和启用

### 1. 确认脚本文件存在

确认下面两个文件已经在仓库中：

- [Save-ClipboardImageToAiCache.ps1](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/Save-ClipboardImageToAiCache.ps1)
- [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)

### 2. 启动 AutoHotkey 脚本

有两种方式。

方式一：双击运行

- 直接双击 [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)

方式二：显式用 v2 运行

```powershell
& "C:\Program Files\AutoHotkey\v2\AutoHotkey.exe" "D:\PersonWorkSpace\PasePictureForKimiCodeAndClaudeCode\script\ai-clipboard-image-paste.ahk"
```

启动后，系统托盘会出现 AutoHotkey 图标，表示热键已生效。

### 3. 验证热键

验证顺序：

1. 按 `Win + Shift + S` 截一张图
2. 切到任意可输入文本的位置
3. 按 `Ctrl + Shift + V`
4. 应插入一段文本，格式类似：

```text
image: "C:\Users\你的用户名\.ai-clipboard-image-cache\shot-20260527-180500.png";
```

再验证非图片场景：

1. 复制一段普通文本
2. 按 `Ctrl + Shift + V`
3. 应表现为普通粘贴

## 如何停止、禁用、解除快捷键

### 临时停止

方法一：

- 在系统托盘找到绿色 AutoHotkey 图标
- 右键
- 选择 `Suspend Hotkeys`

这会临时停用热键，再次点击可恢复。

方法二：

- 右键托盘图标
- 选择 `Exit`

这会彻底结束脚本进程，本次会话内快捷键立即失效。

### 永久取消

如果你之后不想再使用这个快捷键，有三种做法：

- 不再启动 [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)
- 删除你自己创建的开机启动快捷方式
- 直接删掉该 `.ahk` 文件

### 修改热键

如果后续发现 `Ctrl + Shift + V` 与别的软件冲突，修改这个文件即可：

- [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)

当前热键定义是：

```ahk
^+v::HandleEnhancedPaste()
```

其中：

- `^` 表示 `Ctrl`
- `+` 表示 `Shift`

例如改成 `Alt + V`：

```ahk
!v::HandleEnhancedPaste()
```

保存后重新启动脚本即可生效。

## 如何开机自动生效

如果你需要开机自动启用：

1. 按 `Win + R`
2. 输入 `shell:startup`
3. 在打开的启动目录里创建一个快捷方式
4. 快捷方式目标指向：

```text
C:\Program Files\AutoHotkey\v2\AutoHotkey.exe
```

5. 参数填写为：

```text
"D:\PersonWorkSpace\PasePictureForKimiCodeAndClaudeCode\script\ai-clipboard-image-paste.ahk"
```

之后每次登录 Windows，热键都会自动生效。

## 常见问题

### 1. 为什么不用 PowerShell 7

保存截图这一步继续使用 Windows PowerShell 5.1 的 `-Sta`，因为 Windows 剪贴板图片读取在这个路径上更稳。

这和你平时在哪个 shell 里运行 Claude Code / Kimi Code 没冲突。

### 2. 为什么不直接改写剪贴板再粘贴

因为你要求保留原剪贴板内容。

所以当前实现是：

- 读取图片
- 落盘
- 直接发送文本

而不是：

- 把剪贴板替换成路径文本
- 再模拟 `Ctrl + V`

### 3. 为什么不支持“复制的图片文件”

因为你已经确认 Claude Code 和 Kimi Code 对图片文件本身支持普通粘贴。

所以这个工具只处理“截图位图无法直接粘贴”的场景。

## 故障排查

### 现象 1：按 `Ctrl + Shift + V` 没反应

检查：

- [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk) 是否已启动
- 系统托盘里是否有 AutoHotkey 图标
- 当前是否被 `Suspend Hotkeys`

### 现象 2：只出现普通粘贴，没有生成 `image:`

这通常表示当前剪贴板不是图片，而是文本或其他格式。

重新用 `Win + Shift + S` 截图后再试。

### 现象 3：弹出错误框，提示缺少脚本

检查这个文件是否存在：

- [Save-ClipboardImageToAiCache.ps1](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/Save-ClipboardImageToAiCache.ps1)

### 现象 4：生成了路径，但模型读不到图片

检查：

- 图片文件是否真的存在于 `C:\Users\<你>\.ai-clipboard-image-cache\`
- 当前工具是否支持把本地路径当作图片输入

这一步已经超出热键脚本职责。

## 结论

这份文件现在不是设计指南，而是可以直接执行的实施文件。

下一步只剩两件事：

1. 运行 [ai-clipboard-image-paste.ahk](D:/PersonWorkSpace/PasePictureForKimiCodeAndClaudeCode/script/ai-clipboard-image-paste.ahk)
2. 实测 `Ctrl + Shift + V` 是否符合你的日常使用预期
