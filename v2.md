# 基于 `对v1的回复.txt` 的 v2 方案

## 结论

你的补充是有效的，而且把方案进一步收敛到了一个更可落地的版本。

和 `v1` 相比，`v2` 可以明确成下面这件事：

> 保留原生 `Ctrl + V`，新增 `Ctrl + Shift + V`，仅在指定终端窗口中生效；当剪贴板里是图片时，把图片保存到明显可删除的 AI 临时目录里，并向当前终端插入一段普通文本：`图片：<本地路径>；`；整个过程不覆盖原剪贴板内容。

这个版本比 `v1` 更接近真正可实现、可长期使用的方案。

---

## 你的补充里，哪些点很关键

### 1. 不要覆盖剪贴板内容

这是一个很好的新增约束，我同意。

它意味着实现时不能再走这种粗暴路径：

- 把原剪贴板改成提示词文本
- 再发送一次 `Ctrl + V`

因为这样虽然“能工作”，但副作用太明显。

`v2` 应该改成：

- 读取剪贴板中的图片
- 落盘
- 直接向当前输入焦点发送字符串
- 原剪贴板内容保持不变

这是一个正确的收紧。

### 2. `Ctrl + Shift + V` 作为新增快捷键

这个选择基本合理。

从常见 Windows 使用习惯看，`Ctrl + Shift + V` 在很多桌面应用里不是系统级保留热键，因此作为 AutoHotkey 的自定义热键通常可行。

但要注意两点：

- 某些应用自己可能已经定义了它，比如部分终端、编辑器、输入法工具
- 是否冲突，取决于你主要在哪个窗口里用

所以更准确的说法不是“它一定没绑定”，而是：

> 它很适合作为首选默认键，但仍需要在你的实际终端环境里验一次。

如果以后发现冲突，再换成 `Alt + V` 或其他组合也不难。

### 3. 输出格式改成 `图片：xxxx路径；`

这个要求很重要，而且比我在 `v1` 里说的“只贴路径”更贴近你的真实使用方式。

因为你并不是只想得到一个文件名，而是想要一种：

- 看起来像普通文本粘贴
- agent 一眼能理解这是图片输入
- 后续你还可以继续在同一行补充文字

所以 `v2` 的默认输出格式可以定为：

```text
图片：C:\Users\xxx\.ai-cache\...\shot-20260527-171500.png；
```

这个格式的优点是：

- 足够轻
- 可读性好
- 与自然语言输入兼容
- 不强绑定某个模型提示词模板

### 4. 你问“窗口范围还可以选吗”

可以，而且我认为这件事很关键。

AutoHotkey 的热键可以配置成：

- 全局生效
- 仅某些进程窗口生效
- 更细地按窗口标题或控件条件生效

所以“窗口范围”本质上是可配置项，不是固定死的。

---

## 关于窗口范围，我的新想法

我建议 `v2` 把窗口范围分成三档，而不是一开始写死。

### 方案 A：只对独立终端生效

例如：

- `WindowsTerminal.exe`
- `powershell.exe`
- `pwsh.exe`

这是最稳的第一版。

优点：

- 误触发最少
- 不容易影响 codex
- 行为边界清晰

缺点：

- 如果你很多时候在 VS Code 集成终端里用，就不覆盖那部分场景

### 方案 B：再加上 `Code.exe`

这能覆盖 VS Code 集成终端，但风险明显上升。

原因是 `Code.exe` 里有很多区域：

- 编辑器
- 终端
- 搜索框
- 聊天面板
- 插件面板

如果只按进程名判断，热键可能在不该触发的地方也触发。

所以我不建议把 `Code.exe` 放进第一版默认范围。

### 方案 C：允许用户自己维护白名单

这是我看到你回复后新增的一个想法。

最合理的长期方案不是把范围硬编码，而是把“允许生效的窗口进程名”放进一个简单配置里，例如：

- `WindowsTerminal.exe`
- `powershell.exe`
- `pwsh.exe`

之后你想加 `Code.exe`，只改配置，不改主逻辑。

这会让工具更可持续。

---

## `v2` 需要新增强调的实现要求

## 1. “不覆盖剪贴板”不等于“完全不接触剪贴板”

这里要说清楚一个工程实现上的点。

因为图像本来就在剪贴板里，所以程序一定要“读取剪贴板”。你要求的是：

> 不要篡改、不替换、不污染原剪贴板内容

这完全合理。

所以 `v2` 的正确要求应该表述为：

- 允许读取剪贴板
- 不允许把原剪贴板改写成别的文本
- 失败时也不要清空原剪贴板

### 2. 插入文本最好走“直接发送字符串”

既然你明确要求行为像普通文本粘贴，那我建议实现时优先采用：

- 生成字符串
- 直接发送到当前焦点窗口

而不是：

- 先把字符串塞进剪贴板
- 再模拟 `Ctrl + V`

后者虽然简单，但和“不要覆盖剪贴板内容”天然冲突。

### 3. 图片保存目录名要明显可删除

这个补充也很好，我建议目录命名直接体现三层含义：

- 这是 AI 用的
- 这是缓存/临时内容
- 可以删

例如：

```text
C:\Users\<你>\.ai-image-cache\
```

或者：

```text
C:\Users\<你>\.ai-temp-images\
```

我更偏向：

```text
C:\Users\<你>\.ai-image-cache\
```

理由：

- 一眼能看出是 AI 相关
- `cache` 明确表达“可清理”
- 不会像 `temp` 那样让人误以为系统会自动管理

如果你还想再直白一点，也可以用：

```text
C:\Users\<你>\.ai-image-cache-deleteable\
```

但这个名字有点长，实用性一般。

---

## 我看到的新增问题

## 1. 如果剪贴板里不是图片，`Ctrl + Shift + V` 应该怎么办

这是 `v1` 里没定死、但 `v2` 必须明确的点。

我建议默认行为是：

- 如果剪贴板里不是图片，什么都不做
- 或者发出一个轻量提示音

不要回退成普通粘贴。

原因是这个快捷键的语义已经被定义成：

> 把剪贴板里的图片转成路径插入

如果它在不是图片时又偷偷变回普通粘贴，用户心智会变乱。

所以我更建议：

- `Ctrl + V`：永远普通粘贴
- `Ctrl + Shift + V`：只处理图片；不是图片就不处理

## 2. 要不要支持“复制的图片文件”而不只是“截图位图”

你当前描述偏向截图工具产生的剪贴板图片。

但从实用性看，后续很可能还会遇到两种输入：

- 截图工具产生的位图
- 资源管理器里复制的图片文件

`v2` 要不要支持第二种，我建议这样处理：

- `v2` 先把“剪贴板位图截图”作为主目标
- “复制的图片文件路径”可以作为可选增强

因为你当前最核心的痛点是截图，不是文件管理器复制图片。

## 3. 路径字符串是否要加引号

你给的格式是：

```text
图片：xxxx路径；
```

我建议真正落地时用：

```text
图片："C:\Users\xxx\.ai-image-cache\shot-20260527-171500.png"；
```

因为 Windows 路径经常包含空格。加引号更稳，也更利于 agent 识别完整路径边界。

所以我建议把默认模板定成：

```text
图片："%PATH%"；
```

---

## `v2` 的推荐最终定义

## 行为定义

- 保留 `Ctrl + V` 原样
- 新增 `Ctrl + Shift + V`
- 仅在白名单终端窗口生效
- 仅当剪贴板中存在图片时才执行
- 将图片保存到 `C:\Users\<你>\.ai-image-cache\`
- 向当前输入焦点插入文本：`图片："<路径>"；`
- 不改写用户原剪贴板
- 不接管 codex 原生图片粘贴场景

## 第一版窗口白名单

建议默认只包含：

- `WindowsTerminal.exe`
- `powershell.exe`
- `pwsh.exe`

## 第一版失败策略

- 剪贴板不是图片：不执行插入
- 保存失败：不插入任何文本
- 原剪贴板始终不改写

---

## 对 `v1` 的修正结论

`v1` 的主方向仍然成立，但 `v2` 应该把重点从“专用热键”进一步收紧为“专用热键 + 非侵入文本注入 + 白名单窗口 + 明确缓存目录”。

如果用一句话概括 `v2`：

> 这是一个只在指定终端里生效的截图转路径输入器，而不是一个全局粘贴接管器。

---

## 下一步建议

如果继续往下做，我建议下一步不要再写泛化讨论，而是直接进入实现设计：

1. 定义最终目录名
2. 定义最终输出模板
3. 定义第一版窗口白名单
4. 再产出实际可运行的 AutoHotkey v2 与 PowerShell 脚本

我认为现在已经足够进入代码实现阶段了。
